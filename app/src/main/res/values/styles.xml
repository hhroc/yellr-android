<resources>

    <!-- Base application theme. -->
    <style name="AppTheme"
        parent="@android:style/Theme.Holo">
    </style>

    <!--Style for the Splash Screen, Removes ActionBar-->
    <style name="NoActionBar"
        parent="@android:style/Theme.Holo">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--Main Application Style-->
    <style name="YellrTheme"
        parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/YellrActionbar</item>
        <item name="android:actionMenuTextColor">@android:color/white</item>
    </style>
    <style name="YellrActionbar"
        parent="android:Widget.ActionBar">
        <item name="android:titleTextStyle">@style/YellrActionbar.title</item>
        <item name="android:subtitleTextStyle">@style/YellrActionbar.subtitle</item>
        <item name="android:background">@android:color/holo_blue_dark</item>
        <item name="android:height">56dp</item>
        <item name="android:actionButtonStyle">@style/YellrActionbar.button</item>
    </style>
    <style name="YellrActionbar.title"
        parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="YellrActionbar.subtitle"
        parent="TextAppearance.AppCompat.Widget.ActionBar.Subtitle">
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="YellrActionbar.button"
        parent="android:Widget.ActionButton">
        <item name="android:padding">0dp</item>
    </style>

</resources>
